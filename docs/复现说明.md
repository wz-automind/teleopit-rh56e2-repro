# 复现说明

本项目将上游 Teleopit 作为基础，将 RH56E2 相关文件作为 overlay 安装。这样做的好处是：

1. 原版 Teleopit 入口和配置保持不变。
2. RH56E2 版本可以独立升级和回滚。
3. 上游 commit、somehand commit 和本地资源都明确记录在 `manifest.json`。
4. 合并模型的稳定性修复不会污染官方 G1 模型。

不要直接在一个包含其他实验改动的 Teleopit 工作树上执行安装脚本；推荐使用一个干净目录，例如 `~/Teleopit-rh56e2-runtime`。
